{% extends "layout.twig" %}

{% block content %}
	<div class="container mt-5">
		<div class="row">
			<div class="col-md-8 mx-auto">
				<div class="display-4 mb-4">
					{{ entry.title }}
				</div>

				{{ entry.pageCopy }}

			</div>
		</div>
	</div>
{% endblock %}